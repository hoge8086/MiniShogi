<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ctrl="clr-namespace:MiniShogiMobile.Controls"
             x:Class="MiniShogiMobile.Views.MainPage"
             Style="{StaticResource PageStyle}"
             Title="ミニ将棋">
    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Button Text="はじめる" Style="{StaticResource ButtonStyle}"  Command="{Binding StartGameCommand}"/>
        <Button Text="つづきから" Style="{StaticResource ButtonStyle}" Command="{Binding ContinueGameCommand}"/>
        <Button Text="ゲームを作る" Style="{StaticResource ButtonStyle}" Command="{Binding CreateGameCommand}"/>
        <Label Text="aiueo"/>
    </StackLayout>
</ContentPage>