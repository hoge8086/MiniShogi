<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="MiniShogiMobile.Views.PlayGamePage"
             Title="{Binding Title}">
    <StackLayout>
        <Label  Text="Playing"/>
        <StackLayout BindableLayout.ItemsSource="{Binding Board}" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Spacing="0">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <StackLayout BindableLayout.ItemsSource="{Binding .}" Orientation="Horizontal" VerticalOptions="FillAndExpand" Spacing="0">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <Button Text="{Binding Koma.Value.Name}" BorderColor="Black" FontSize="Large" HorizontalOptions="FillAndExpand"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </StackLayout>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
    </StackLayout>
</ContentPage>