<?xml version="1.0" encoding="utf-8" ?>
<popups:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:popups="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             Style="{StaticResource PopupPageStyle}"
             x:Class="MiniShogiMobile.Views.SaveGamePopupPage">
    <Frame CornerRadius="8" Padding="0" VerticalOptions="Center" HorizontalOptions="Center" WidthRequest="300">
        <StackLayout BackgroundColor="white" >
            <StackLayout Orientation="Horizontal" Margin="15">
                <Label Text="名前" HorizontalOptions="Start" VerticalOptions="Center"/>
                <Entry HorizontalOptions="FillAndExpand" VerticalTextAlignment="Center"/>
            </StackLayout>
            <Grid
                ColumnDefinitions="*,*"
                ColumnSpacing="1"
                BackgroundColor="Gray">
                <Button Grid.Column="0" Command="{Binding CancelCommand}" Text="キャンセル" VerticalOptions="FillAndExpand" CornerRadius="0"/>
                <Button Grid.Column="1" Command="{Binding OkCommand}" Text="保存" VerticalOptions="FillAndExpand" CornerRadius="0"/>
            </Grid>
        </StackLayout>
    </Frame>
</popups:PopupPage>