<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:ctrl="clr-namespace:MiniShogiMobile.Controls"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="MiniShogiMobile.Views.CreateKomaListPage"
             Title="駒作成">
        <Grid RowDefinitions="Auto,*">
            <StackLayout Grid.Row="0" Orientation="Horizontal">
                
                
            </StackLayout>
            <CollectionView Grid.Row="1"
                            Margin="15"
                            ItemsLayout="VerticalGrid, 4"
                            HeightRequest="400"
                            SelectedItem="{Binding SelectedKomaName.Value}"
                            ItemsSource="{Binding KomaNameList}"
                            SelectionMode="Single" >
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <ContentView HorizontalOptions="Center" VerticalOptions="Center">
                            <ctrl:KomaView HeightRequest="80" WidthRequest="80" DisplayName="{Binding .}"/>
                        </ContentView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
</ContentPage>