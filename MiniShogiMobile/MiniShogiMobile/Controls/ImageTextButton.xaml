<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:local="clr-namespace:MiniShogiMobile.Controls"
             x:Name="self"
             x:Class="MiniShogiMobile.Controls.ImageTextButton">
  <ContentView.Content>
        <Grid RowDefinitions="*,Auto"
               xct:TouchEffect.Command="{Binding Path=Command, Source={x:Reference self}}">
                <local:TintImage Source="{Binding Source, Source={x:Reference self}}"
                        TintColor="{Binding TintColor, Source={x:Reference self}}"
                        Grid.Row="0"
                        VerticalOptions="Center"
                        HorizontalOptions="Center"/>
                <Label Text="{Binding Text, Source={x:Reference self}}"
                       Grid.Row="1"
                       TextColor="{Binding TintColor, Source={x:Reference self}}"
                       FontSize="Micro"
                       VerticalOptions="Center"
                       HorizontalOptions="Center"/>
        </Grid>
    </ContentView.Content>
</ContentView>