<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:fft="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations"
             x:Name="self"
             x:Class="MiniShogiMobile.Controls.ImageTextButton">
  <ContentView.Content>
        <Grid>
            <StackLayout Orientation="Horizontal"
                       xct:TouchEffect.Command="{Binding Path=Command, Source={x:Reference self}}"
                       VerticalOptions="CenterAndExpand"
                       HorizontalOptions="FillAndExpand">
                <ff:CachedImage Source="{Binding Source, Source={x:Reference self}}"
                    VerticalOptions="Center"
                    HorizontalOptions="Start">
                    <ff:CachedImage.Transformations>
                        <fft:TintTransformation HexColor="#ffffff" EnableSolidColor="True"/>
                    </ff:CachedImage.Transformations>
                </ff:CachedImage>
                <Label Text="{Binding Text, Source={x:Reference self}}"
                       TextColor="White"
                       FontSize="Small"
                       VerticalOptions="Center"
                       HorizontalOptions="FillAndExpand"/>
            </StackLayout>
            <!--
            <BoxView Grid.Row="1" BackgroundColor="{StaticResource AppControlDarkColor}" HeightRequest="1" HorizontalOptions="FillAndExpand" />
            -->
        </Grid>
    </ContentView.Content>
</ContentView>